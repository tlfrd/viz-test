<%= form_for(experiment) do |f| %>
  <% if experiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(experiment.errors.count, "error") %> prohibited this experiment from being saved:</h2>

      <ul>
      <% experiment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :class => "form-control" %>
  </div>

  <% if false %>
    <div class="field">
      <%= f.label :experiment_tasks %>
      <%= collection_check_boxes(:experiment_tasks, :task_ids, Task.all, :id, :name) %>
    </div>
  <% end %>

  <br/>

  <div class="actions">
      <%= f.submit :class => 'btn btn-success' %>
  </div>
<% end %>
